<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.its.sikemastc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppMaterialTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.dosen.HalamanUtamaDosen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NavigationBar" />
        <activity android:name=".ActionBarToolbar" />
        <activity android:name=".activity.dosen.LihatKehadiran" />
        <activity android:name=".activity.dosen.ListKelasDiampu" />
        <activity android:name=".activity.dosen.PenjadwalanUlang"></activity>
    </application>

</manifest>